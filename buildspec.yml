version: 0.2

phases:
  install:
    commands:
      - echo "No Docker needed, just simple build"
  build:
    commands:
      - echo "Copying files to S3..."
      - aws s3 sync . s3://your-bucket-name --delete
